<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ChakulaConnect" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryDark</item>
        <item name="colorPrimaryVariant">#333333</item>
        <item name="colorOnPrimary">@color/primaryDarkLight</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primaryDarkLighter</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="homeAsUpIndicator">@drawable/custom_back_icon</item>
        <item name="android:textColor">@color/LightGrey</item>
        <item name="android:textCursorDrawable">@drawable/custom_cursor</item>

    </style>
    <style name="SplashScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">?attr/colorPrimary</item>
    </style>
    <style name="ToolbarTheme" parent="Widget.AppCompat.Toolbar">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:background">@color/primaryDark</item>
    </style>
    <style name="MainActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/primaryDark</item>
    </style>
    <style name="NavigationViewTheme" parent="Widget.Design.NavigationView">
        <!-- Customize attributes specific to NavigationView -->
        <item name="android:background">@color/primaryDark</item>
        <item name="itemIconTint">@color/LightGrey</item>
        <item name="itemTextColor">@color/LightGrey</item>
        <item name="android:textColor">@color/LightGrey</item>
    </style>
    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorHeight">4dp</item> <!-- Customize indicator height -->
        <item name="android:background">@color/primaryDark</item>
        <item name="tabIndicatorGravity">bottom</item>
        <item name="tabTextAppearance">@style/CustomTabTextAppearance</item> <!-- Customize text appearance -->
    </style>

    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">12sp</item> <!-- Customize text size -->
    </style>
</resources>